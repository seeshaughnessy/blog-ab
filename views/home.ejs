<%- include('./partials/header') %>

<h1>Home</h1>

<% posts.forEach(post => { %>

<h2><%= post.title %></h2>
<p>
  <%= truncate(post.body) %>
  <a href="/posts/<%= post.title %>"> [more]</a>
</p>

<% }); %> <%- include('./partials/footer') %>
